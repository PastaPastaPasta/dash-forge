{
  "name": "glob_brace_is_literal_not_alternation",
  "description": "'{a,b}' is literal (git wildmatch has no brace alternation): must NOT alternate.",
  "case": "matches_protected",
  "input": {
    "refName": "refs/heads/main",
    "patterns": [
      "refs/heads/{main,dev}"
    ]
  },
  "expected": false
}
